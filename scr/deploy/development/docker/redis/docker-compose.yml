version: "3"
services:
  redis:
    restart: always
    image: redis:latest
    container_name: simple-admin-redis
    ports:
      - "6379:6379"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - /etc/localtime:/etc/localtime
      - /etc/timezone:/etc/timezone
    networks:
      simple-admin-net:
networks:
  simple-admin-net:
    external:
      name: simple-admin-net
